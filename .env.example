PORT=8787
DB_PATH=./data/contentdashboard.sqlite
INTAKE_TOKEN=change-me-to-a-long-random-secret

# Dev only — Vite dev server origin for CORS
CLIENT_ORIGIN=http://localhost:5173

# Production: set NODE_ENV=production and run `npm start`
# The server will serve the built frontend from ./dist automatically
